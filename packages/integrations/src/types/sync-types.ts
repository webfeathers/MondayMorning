/**
 * Supporting types for CRM integration
 *
 * These types will be fully implemented in Task 3.3.
 * For now, they are placeholders to allow the CRMProvider interface to compile.
 */

// Authentication types
export type AuthResult = unknown;

// Schema discovery types
export type ObjectSchema = unknown;
export type FieldSchema = unknown;
export type ObjectMetadata = unknown;

// Sync operation types
export type SyncOptions = unknown;
export type SyncResult<T> = unknown;

// Raw record types
export type RawDeal = unknown;
export type RawAccount = unknown;
export type RawContact = unknown;
export type RawTicket = unknown;

// Rate limiting types
export type RateLimitStatus = unknown;

// Webhook types
export type WebhookRegistration = unknown;
